{
  "name": "afghan-translator-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "static": "next build && next export",
    "sync": "next build && next export && npx cap sync && npx cap open ios",
    "ionic:build": "npm run build",
    "ionic:serve": "npm run start"
  },
  "dependencies": {
    "@capacitor/core": "^5.2.3",
    "@capacitor/ios": "^5.2.3",
    "capacitor-rate-app": "^4.0.3",
    "next": "13.4.19",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "sharp": "^0.32.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.2.3"
  }
}
